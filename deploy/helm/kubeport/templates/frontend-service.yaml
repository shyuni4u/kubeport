apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeport.frontend.fullname" . }}
  labels:
    {{- include "kubeport.frontend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "kubeport.frontend.selectorLabels" . | nindent 4 }}
