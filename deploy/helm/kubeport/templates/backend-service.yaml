apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeport.backend.fullname" . }}
  labels:
    {{- include "kubeport.backend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "kubeport.backend.selectorLabels" . | nindent 4 }}
